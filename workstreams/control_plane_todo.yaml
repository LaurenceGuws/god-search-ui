items:
  - id: CP-01
    title: Align control-plane docs with actual command behavior
    status: completed
    owner: shell
    files:
      - src/main.zig
      - src/ipc/
      - docs/architecture/WA1_CONTROL_PLANE_SPEC.md
    done:
      - Control commands are documented in one canonical spec.
      - Removed unsupported command names from control-plane request schema and command lists.
      - Verified CLI usage text already matches implemented command set.
    todo:
      - None.
    notes: Keep this focused on behavior clarity, not feature expansion.
