items:
  - id: UI-01
    title: Finish generic preview and popup readability polish
    status: in_progress
    owner: shell
    files:
      - src/ui/gtk/preview.zig
      - src/ui/gtk/bootstrap.zig
      - src/ui/gtk/styles.zig
    done:
      - Generic preview model is in place.
      - Grep hit highlighting and directory mode toggle are implemented.
    todo:
      - Improve popup content layout readability.
      - Clean visual polish for preview pager spacing and contrast.
    notes: Do not reintroduce structured preview field coupling.
